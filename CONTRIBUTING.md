# Contributing

Thanks for improving `lockfile-change-explainer`.

## Local checks

Run the test suite before opening a pull request:

```bash
python -m unittest discover -s tests
```

Keep dependencies stdlib-first unless a new dependency is clearly justified.

Release instructions live in [PUBLISHING.md](PUBLISHING.md).
